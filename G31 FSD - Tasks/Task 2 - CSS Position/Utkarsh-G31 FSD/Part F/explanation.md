# Explanation how z-index affects stacking order.

The z-index property decides which element appears on top when elements overlap. An element with a higher z-index value appears above an element with a lower z-index value.